import type { Tool } from "./tool.js";

// Placeholder function for hover tool, accepting the flag
export function hover(captureSnapshot: boolean): Tool[] {
    // TODO: Implement hoverTool and potentially use flag
    return [];
}
export default hover; 