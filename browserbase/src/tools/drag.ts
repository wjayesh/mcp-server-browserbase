import type { Tool } from "./tool.js";

// Placeholder function for drag/drop tools, accepting the flag
export function drag(captureSnapshot: boolean): Tool[] {
    // TODO: Implement dragTool and potentially use flag
    return [];
}
export default drag; 