import type { Tool } from "./tool.js";

// Placeholder function for select option tool, accepting the flag
export function selectOption(captureSnapshot: boolean): Tool[] {
    // TODO: Implement selectOptionTool and potentially use flag
    return [];
}
export default selectOption; 